# ReMEMBER 20:9 Viewport Fit Implementation Plan

**Goal:** Render the complete ReMEMBER animation inside one 20:9 landscape viewport without scrolling or clipped panel content.

**Architecture:** Keep the self-contained animation and phase controller. Use a three-column grid for chat, gaps, and history, with the summary below the two right columns. Verify the real browser geometry at 800x360 and 700x315 and verify the publication wrapper at the same ratio.

## Constraints

- Preserve every authored message and the existing 22-second interaction sequence.
- Use an exact `20 / 9` publication container, expanded up to 800px for readability.
- Do not introduce internal or page-level scrolling.

## Execution

- [x] Update the browser contract to 800x360 and 700x315 and confirm the portrait layout fails.
- [x] Convert the animation to the three-column landscape grid.
- [x] Change the publication wrapper to `aspect-ratio: 20 / 9` and widen only the animation entry.
- [x] Run the Chrome geometry test and inspect the 800x360 final frame.
- [ ] Run the full Hugo build when the `hugo` executable is available.
